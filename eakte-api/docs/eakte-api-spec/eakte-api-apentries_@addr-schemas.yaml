components:
  schemas:

    ReadApentryAntwortDTO:
      type: object
      properties:
        giobjecttype:
          type: array
          description: |
            Liste der Vorgänge der Akte mit jeweils Bezeichner (Name) und COO-Adresse (ID) des Objekts
          items:
            $ref: 'eakte-api--global-schemas.yaml#/components/schemas/Objektreferenz'
