components:
  schemas:

    UpdateBusinessDataValueDTO:
      required:
        - reference
      type: object
      properties:
        reference:
          type: string
          description: |
            Referenz des Fachdatums\
            Frage: Wo bekommt man den Namen des Fachdatums her?
          example: data_dognumber
        value:
          type: string
          description: Wert des Fachdatums
          example: '458965'
