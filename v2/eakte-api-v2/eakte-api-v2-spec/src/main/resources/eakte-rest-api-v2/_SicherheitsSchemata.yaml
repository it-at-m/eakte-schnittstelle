components:
  securitySchemes:

    basicAuth:
      type: http
      scheme: basic
      description: |
        Diese API erlaubt die Authentisierung mittels des HTTP-Headers
        'Autthorization' und der Variante 'Basic' 
        [Weitere Information siehe https://api.example.com/docs/auth](https://api.example.com/docs/auth)

    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: jwt
